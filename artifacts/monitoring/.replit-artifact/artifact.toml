kind = "web"
previewPath = "/"
title = "Trademark Monitoring"
version = "1.0.0"
id = "artifacts/monitoring"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19592

[services.development]
run = "pnpm --filter @workspace/monitoring run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/monitoring", "run", "build" ]
publicDir = "artifacts/monitoring/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19592"
BASE_PATH = "/"
